export interface FilePrivileges {
  readable: boolean;
  writable: boolean;
  commentable: boolean;
}

export interface ProjectPrivileges {
  readable: boolean;
  writable: boolean;
  administrable: boolean;
}
